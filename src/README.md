# src

Rust source for `tx_agent`.

- `bin/` contains binary launchers and the report generator.
- `core/` contains Solana/Jito infrastructure, lifecycle logging, networking, and the MCP server.
- `ai/` contains the bounded decision layer, policy validation, and MCP client host.
